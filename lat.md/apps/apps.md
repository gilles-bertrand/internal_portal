# Apps

Documentation des applications composées (`@apps/front`, `@apps/backend`, `@apps/e2e`) qui assemblent les libs `*-front`/`*-backend`.

- [[front-integration]] — comment les libs `*-front` se branchent dans l'app Ember hôte
- [[backend-bootstrap]] — composition root backend, assemblage des modules, secrets et seeding
- [[e2e-strategy]] — stratégie de test de bout en bout Playwright
